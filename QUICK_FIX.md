# إصلاح سريع لمشكلة فشل بدء الجلسة من أجهزة أخرى

## المشكلة
❌ عند فتح التطبيق من هاتف ذكي أو جهاز آخر، يظهر: **"فشل بدء الجلسة"**

## الحل السريع (3 خطوات فقط)

### 1️⃣ افتح ملف `.env.local`
```bash
# ابحث عن هذه السطور:
NEXT_PUBLIC_API_BASE_URL=http://192.168.0.30:3003
NEXT_PUBLIC_API_URL=http://192.168.0.30:3003
```

### 2️⃣ تحقق من IP الخادم
افتح Terminal/Command Prompt وشغل:

**Windows:**
```cmd
ipconfig
```

**Mac/Linux:**
```bash
ifconfig | grep inet
```

البحث عن IP يبدأ بـ `192.168.x.x` أو `10.0.x.x`

### 3️⃣ حدّث عنوان IP إذا لزم الأمر
إذا كان IP الخادم مختلفاً، استبدله:
```
NEXT_PUBLIC_API_BASE_URL=http://<IP_الخادم_الحقيقي>:3003
NEXT_PUBLIC_API_URL=http://<IP_الخادم_الحقيقي>:3003
```

ثم:
```bash
npm run dev
```

---

## اختبر الآن

### من نفس الجهاز ✅
```
http://localhost:3000
```

### من جهاز آخر على الشبكة ✅
```
http://192.168.0.29:3000
```

---

## في حالة الاستمرار في المشكلة:

### تحقق من الخادم
```bash
# اختبر ما إذا كان الخادم يستجيب
ping 192.168.0.30

# اختبر API الخادم
curl http://192.168.0.30:3003/api/health
```

### افتح Browser Console (`F12`)
- ابحث عن الأخطاء الحمراء
- اختبر الاتصال بـ Socket: `socket.connected`

### 🔴 إذا رأيت هذه الأخطاء:

| الخطأ | الحل |
|------|------|
| `Cannot connect` | الخادم غير مشغل أو IP غير صحيح |
| `Connection refused` | المنفذ 3003 مغلق أو محجوب |
| `Timeout` | الجهاز غير متصل بالشبكة الصحيحة |
| `CORS error` | الخادم لم يتم إعداده لقبول طلبات من هذا الجهاز |

---

## نصائح مهمة 💡

✅ **استخدم:**
- IP الحقيقي للخادم (مثل `192.168.0.30`)
- نفس الشبكة لجميع الأجهزة

❌ **لا تستخدم:**
- `localhost` - لا يعمل من أجهزة أخرى
- `127.0.0.1` - عنوان محلي فقط
- أسماء النطاقات الخارجية في الشبكة المحلية

---

## تحقق من التحسينات المضافة:

✨ **التحسينات الجديدة:**
- رسائل خطأ أكثر وضوحاً
- إعادة محاولة الاتصال تلقائياً
- دعم أفضل للأجهزة المتعددة
- تسجيل مفصل للأخطاء

📄 للتفاصيل الكاملة، اقرأ: [`NETWORK_CONFIG_GUIDE.md`](NETWORK_CONFIG_GUIDE.md)
