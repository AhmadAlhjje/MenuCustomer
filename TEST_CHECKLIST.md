# قائمة اختبار الوصول من أجهزة متعددة

## ✅ قائمة التحقق قبل الاختبار

- [ ] الخادم Backend يعمل على المنفذ 3003
- [ ] التطبيق الأمامي يعمل على المنفذ 3000
- [ ] جميع الأجهزة متصلة بنفس شبكة WiFi
- [ ] IP الخادم صحيح في `.env.local`
- [ ] لم يتم تعديل أي ملفات أخرى من قبل الاختبار

## 🧪 خطوات الاختبار

### المرحلة 1: اختبار الاتصال الأساسي

#### 1.1 على جهاز الخادم:
```bash
# افتح Terminal/Command Prompt
# تحقق من IP الجهاز
ipconfig  # Windows
ifconfig  # Mac/Linux

# لاحظ الـ IP (مثلاً: 192.168.0.30)
```
- [ ] IP مرئي ويبدأ بـ `192.168.x.x`

#### 1.2 على جهاز العميل (الجهاز الآخر):
```bash
# اختبر إذا كنت تستطيع الوصول إلى الخادم
ping 192.168.0.30
```
- [ ] استجابة ping ناجحة

#### 1.3 اختبر الخادم عبر curl:
```bash
curl http://192.168.0.30:3003/api/health
```
- [ ] استجابة من الخادم (ليس "connection refused")

### المرحلة 2: اختبار التطبيق الأمامي

#### 2.1 على جهاز الخادم:
```
افتح المتصفح: http://localhost:3000
```
- [ ] التطبيق يفتح بنجاح
- [ ] لا توجد أخطاء حمراء في Console

#### 2.2 على جهاز العميل:
```
افتح المتصفح: http://192.168.0.29:3000
(استبدل 192.168.0.29 بـ IP الجهاز الأول)
```
- [ ] التطبيق يفتح بنجاح
- [ ] لا توجد أخطاء حمراء في Console

#### 2.3 افتح Developer Tools (F12):
- [ ] انتقل إلى Console tab
- [ ] لاحظ أول رسالة تتعلق بـ "API Client"
- [ ] تحقق من أن الـ URL صحيح

### المرحلة 3: اختبار بدء الجلسة

#### 3.1 على جهاز العميل:
1. افتح التطبيق: `http://192.168.0.29:3000`
2. حضّر كود QR من الخادم (إن أمكن)
3. ادخل عدد الضيوف (مثلاً: 2)
4. انقر على "بدء الجلسة"

#### النتائج المتوقعة:
- [ ] لا توجد رسالة خطأ في الـ UI
- [ ] يتم إعادة التوجيه إلى صفحة المقائم
- [ ] في Console، لا توجد رسائل خطأ حمراء

#### إذا فشل:
- [ ] لاحظ رسالة الخطأ بالضبط
- [ ] انسخ الأخطاء من Console
- [ ] راجع قسم "استكشاف الأخطاء" أدناه

### المرحلة 4: اختبار الميزات الأساسية

بعد نجاح بدء الجلسة:

#### 4.1 عرض المقائم:
- [ ] تحميل المقائم بنجاح
- [ ] تحميل الصور بنجاح
- [ ] عرض الأصناف والأطعمة

#### 4.2 إضافة طلب:
- [ ] نقر على عنصر من الطعام
- [ ] إضافة عنصر إلى الطلب
- [ ] عرض الطلب بنجاح

#### 4.3 الاتصال بالخادم:
- [ ] في Console، لاحظ رسائل التصحيح
- [ ] تحقق من أن الطلبات تصل إلى الخادم الصحيح

## 🔴 استكشاف الأخطاء

### الخطأ 1: "Cannot connect to server"

**الأسباب المحتملة:**
1. الخادم غير مشغل
2. IP غير صحيح في `.env.local`
3. المنفذ 3003 مغلق

**الحل:**
```bash
# تحقق من أن الخادم يعمل
curl http://192.168.0.30:3003/api/health

# تحقق من IP الخادم الحقيقي
ipconfig
```

### الخطأ 2: "QR Code invalid"

**الأسباب المحتملة:**
1. كود QR انتهت صلاحيته
2. كود QR من جلسة أخرى

**الحل:**
- استخدم كود QR جديد من الخادم

### الخطأ 3: "Connection refused"

**الأسباب المحتملة:**
1. الخادم غير مشغل على المنفذ 3003
2. جدار الحماية يحجب المنفذ

**الحل:**
```bash
# تحقق من أن الخادم يستمع
netstat -ano | findstr :3003  # Windows
lsof -i :3003  # Mac/Linux

# أو اختبر من سطر الأوامر
curl http://192.168.0.30:3003/api/health
```

### الخطأ 4: "Timeout"

**الأسباب المحتملة:**
1. الشبكة بطيئة
2. الخادم لا يستجيب بسرعة
3. قطع في الاتصال

**الحل:**
- أعد محاولتك بعد ثوانٍ قليلة
- تحقق من اتصال WiFi

## 📊 ملخص النتائج

| الاختبار | النتيجة | ملاحظات |
|--------|--------|---------|
| Ping للخادم | ✅/❌ | |
| curl للخادم | ✅/❌ | |
| التطبيق محلياً | ✅/❌ | |
| التطبيق من جهاز آخر | ✅/❌ | |
| بدء الجلسة | ✅/❌ | |
| عرض المقائم | ✅/❌ | |
| إضافة طلب | ✅/❌ | |

## 📝 ملاحظات إضافية

- **لا تغير أي شيء في الكود** أثناء الاختبار
- **احفظ جميع الأخطاء** التي تراها للإشارة إليها لاحقاً
- **استخدم نفس Browser** (Chrome, Firefox, Safari)
- **امسح Cache** إذا غيرت IP: Ctrl+Shift+Delete

## ✨ نصائح مفيدة

### لرؤية جميع الأخطاء بوضوح:
1. افتح Developer Tools: F12
2. انتقل إلى Console
3. اكتب: `console.clear()` لحذف الرسائل القديمة
4. أعد الاختبار

### لاختبار سرعة الاتصال:
1. افتح Network tab في Developer Tools
2. أعد تحميل الصفحة
3. لاحظ أوقات الاستجابة

### لمراقبة اتصال Socket:
1. في Console اكتب: `socket.connected`
2. يجب أن ترى `true` إذا كان الاتصال نشطاً

---

بعد إكمال جميع الاختبارات بنجاح، يجب أن يعمل التطبيق بشكل طبيعي من أي جهاز على الشبكة!
