# 📱 دليل ماسح QR Code - QR Scanner Guide

## 🎯 نظرة عامة / Overview

تم إضافة ميزة **مسح QR Code** داخل التطبيق لتسهيل بدء الجلسة.

A **QR Code Scanner** feature has been added to make it easier to start sessions.

---

## 📦 التثبيت / Installation

### 1. تثبيت المكتبة المطلوبة
```bash
npm install react-qr-scanner
```

هذه المكتبة مضافة بالفعل في `package.json`.

### 2. تثبيت جميع المكتبات
```bash
cd customer-app
npm install
```

---

## 🚀 كيفية الاستخدام / How to Use

### طريقتان لبدء الجلسة / Two Ways to Start Session:

#### الطريقة 1: مسح QR من داخل التطبيق (جديد!)
1. افتح التطبيق على `http://localhost:3000`
2. اضغط على زر **"مسح رمز QR" / "Scan QR Code"**
3. اسمح للتطبيق بالوصول للكاميرا
4. وجّه الكاميرا نحو QR Code
5. سيتم تحويلك تلقائياً إلى صفحة بدء الجلسة

#### الطريقة 2: مسح QR بكاميرا الهاتف العادية (التقليدية)
1. افتح كاميرا الهاتف العادية
2. امسح QR Code المطبوع على الطاولة
3. سيفتح الرابط في المتصفح مباشرة
4. أدخل عدد الأشخاص وابدأ الجلسة

---

## 📂 الملفات المضافة / New Files

### 1. صفحة ماسح QR
**المسار:** `src/app/scan-qr/page.tsx`

صفحة كاملة لمسح QR Code:
- تفعيل الكاميرا
- قراءة الرمز
- التوجيه التلقائي

### 2. زر ماسح QR
**المسار:** `src/components/molecules/QRScannerButton.tsx`

مكون قابل لإعادة الاستخدام يمكن وضعه في أي مكان.

### 3. تحديثات الترجمة
- `src/translations/ar.json` - نصوص عربية جديدة
- `src/translations/en.json` - نصوص إنجليزية جديدة

النصوص المضافة:
```json
{
  "common": {
    "or": "أو / OR",
    "startScanning": "بدء المسح / Start Scanning"
  },
  "session": {
    "scanQRCode": "مسح رمز QR / Scan QR Code",
    "orEnterManually": "أو أدخل الرمز يدوياً / Or enter code manually",
    "useDemo": "استخدام النسخة التجريبية / Use Demo Version"
  }
}
```

---

## 🎨 التصميم / Design

### الصفحة الرئيسية المحدثة

```
┌────────────────────────────┐
│      👥 أيقونة ترحيب        │
│      مرحباً / Welcome       │
│   امسح QR للبدء             │
├────────────────────────────┤
│  [مسح رمز QR 📷]           │  ← زر جديد
├────────────────────────────┤
│         ── أو ──            │
├────────────────────────────┤
│  [استخدام النسخة التجريبية]│
└────────────────────────────┘
```

### صفحة الماسح

```
┌────────────────────────────┐
│   مسح رمز QR               │
├────────────────────────────┤
│ ┌────────────────────────┐ │
│ │                        │ │
│ │   📷 عرض الكاميرا      │ │
│ │                        │ │
│ └────────────────────────┘ │
├────────────────────────────┤
│      [إلغاء]               │
├────────────────────────────┤
│  أو أدخل الرمز يدوياً       │
│  [استخدام النسخة التجريبية]│
└────────────────────────────┘
```

---

## ⚙️ الإعدادات / Settings

### أذونات الكاميرا / Camera Permissions

التطبيق يطلب إذن الوصول للكاميرا تلقائياً.

**في المتصفح:**
- Chrome: يظهر إشعار في الأعلى
- Safari: يطلب الإذن مرة واحدة
- Firefox: يطلب الإذن عند كل استخدام

**على الهاتف:**
- قد تحتاج لتفعيل أذونات الكاميرا في إعدادات النظام

---

## 🔧 استكشاف الأخطاء / Troubleshooting

### المشكلة: الكاميرا لا تعمل
**الحلول:**
1. تحقق من أذونات الكاميرا في المتصفح
2. جرب متصفح آخر
3. تحقق من أن الكاميرا تعمل في تطبيقات أخرى
4. استخدم HTTPS (مطلوب للكاميرا في production)

### المشكلة: لا يقرأ الرمز
**الحلول:**
1. تأكد من وضوح الإضاءة
2. امسح العدسة
3. اقترب/ابتعد عن الرمز
4. استخدم "النسخة التجريبية" بدلاً

### المشكلة: خطأ في البناء
```bash
# امسح وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

---

## 📱 HTTPS في Production

**مهم جداً:** الكاميرا تعمل فقط على HTTPS في production!

### التطوير المحلي (HTTP)
```bash
npm run dev
# يعمل على http://localhost:3000
```

### الإنتاج (HTTPS مطلوب)
عند النشر على:
- **Vercel** - HTTPS تلقائي ✅
- **Netlify** - HTTPS تلقائي ✅
- **VPS خاص** - يجب تفعيل SSL Certificate

---

## 🎯 حالات الاستخدام / Use Cases

### 1. مطعم تقليدي
- QR Code مطبوع على كل طاولة
- الزبون يمسح بكاميرا الهاتف
- يفتح التطبيق مباشرة

### 2. طلبات عبر الإنترنت
- الزبون يفتح التطبيق
- يضغط "مسح QR"
- يمسح الرمز من منيو PDF أو صورة

### 3. التجربة
- لا يوجد QR Code؟
- استخدم "النسخة التجريبية"
- تُفتح جلسة تجريبية مباشرة

---

## 🔄 تدفق العمل الكامل / Complete Flow

```
الزبون يفتح التطبيق
    ↓
يرى خيارين:
  1️⃣ مسح QR Code
  2️⃣ النسخة التجريبية
    ↓
إذا اختار "مسح QR":
    ↓
صفحة /scan-qr تُفتح
    ↓
طلب إذن الكاميرا
    ↓
الزبون يوافق
    ↓
الكاميرا تُفعّل
    ↓
يوجه الهاتف نحو QR
    ↓
الماسح يقرأ الرمز
    ↓
استخراج qrCode من الرابط
    ↓
التحويل إلى: /table/{qrCode}
    ↓
إدخال عدد الأشخاص
    ↓
بدء الجلسة
    ↓
الانتقال إلى القائمة
```

---

## 🔐 الأمان / Security

### الأذونات
- الكاميرا: مطلوبة فقط في صفحة المسح
- يتم طلب الإذن عند الضرورة
- لا يتم حفظ الصور

### الخصوصية
- لا يتم تسجيل الفيديو
- البيانات تُعالج محلياً فقط
- لا إرسال لأي خادم خارجي

---

## 📊 الإحصائيات / Statistics

### الملفات المضافة
- ✅ `src/app/scan-qr/page.tsx` - صفحة الماسح
- ✅ `src/components/molecules/QRScannerButton.tsx` - زر الماسح
- ✅ تحديثات `package.json` - المكتبة
- ✅ تحديثات `ar.json` و `en.json` - الترجمات
- ✅ تحديثات `src/app/page.tsx` - الصفحة الرئيسية

### المكتبة المستخدمة
- **الاسم:** react-qr-scanner
- **الإصدار:** 1.0.0-alpha.11
- **الحجم:** ~30 KB
- **الترخيص:** MIT

---

## 💡 نصائح للاستخدام الأمثل

### للمطورين
1. استخدم `dynamic import` لتجنب SSR errors
2. أضف loading state أثناء تحميل الماسح
3. اعرض رسالة خطأ واضحة إذا فشل الوصول للكاميرا

### للمستخدمين
1. تأكد من السماح بالوصول للكاميرا
2. استخدم إضاءة جيدة
3. ثبت الهاتف عند المسح
4. إذا لم ينجح، استخدم "النسخة التجريبية"

---

## 🔮 المستقبل / Future Enhancements

### ميزات محتملة:
- [ ] مسح متعدد للأكواد
- [ ] حفظ الأكواد الممسوحة
- [ ] دعم باركود عادي
- [ ] إضاءة الفلاش للمسح الليلي
- [ ] تكبير/تصغير الكاميرا

---

## 📞 الدعم / Support

إذا واجهت مشاكل:
1. راجع قسم "استكشاف الأخطاء" أعلاه
2. تحقق من console المتصفح
3. جرب على متصفح مختلف
4. استخدم الطريقة التقليدية (QR الهاتف)

---

## ✅ الخلاصة / Summary

الآن التطبيق يدعم طريقتين لبدء الجلسة:

1. **داخل التطبيق:** زر "مسح QR" يفتح الكاميرا
2. **خارج التطبيق:** مسح QR بكاميرا الهاتف العادية

كلا الطريقتين تعملان بشكل مثالي!

---

**تاريخ الإنشاء:** 2025
**الإصدار:** 1.1.0 (مع ماسح QR)
**الحالة:** ✅ جاهز للاستخدام
